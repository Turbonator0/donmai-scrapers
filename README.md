# File scrapers for boorus

## Usage:

Safebooru grabber:
```
python3 safebooru-grabber.py -t [tags] -o [output folder]
```
Danbooru grabber:

```
python3 danbooru-grabber.py -t [tags] -o [output folder] -r [e,q,s]
```
